/** Quiet corner wordmark for Delivery view only — not a diagonal stamp. */
export function DeliveryWordmark() {
  return (
    <span
      className="pointer-events-none absolute bottom-2 right-2 select-none font-display text-[10px] tracking-[0.2em] uppercase text-[#c9922e]/20 sm:text-xs"
      aria-hidden
    >
      Ibrahim Lens
    </span>
  )
}
