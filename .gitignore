# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env & secrets
.env
.env.production
.env.*.local
deploy/.env

# PocketBase data (DB + uploads) â€” backup separately; never commit
pb_data/

# Volume-seed booking-questions backup (local only)
scripts/.volume-booking-questions.json

# Instagram one-shot seed stills (local demo only)
scripts/seed-assets/instagram/*
!scripts/seed-assets/instagram/.gitkeep
!scripts/seed-assets/instagram/.gitignore

# Local PocketBase binary for machines without Docker (optional)
tools/pocketbase/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Superdesign local state
.superdesign/tmp/
.superdesign/resume.json

# Scratch assistant review dumps
docs/_line*.json
docs/_review*.txt
docs/_written_*


# Local PocketBase backups — never commit
pb_data-backup*.zip
*.zip

