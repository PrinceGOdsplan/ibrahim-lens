## 1. Primitives

- [x] 1.1 Add `Textarea` matching `Input` underline + shared focus ring; point `quietTextareaClass` at it
- [x] 1.2 Add form + toolbar `Select` (panel fill, shared focus, 44px form hit)
- [x] 1.3 Quiet default `Label` (muted xs) so Website/Bookings/Clients match without per-field overrides
- [x] 1.4 Align `PhoneNgInput` studio tone with underline neighbors; keep public tone

## 2. Migrate hubs

- [x] 2.1 Bookings + booking questions + Clients: textarea/select/phone use primitives
- [x] 2.2 WorkView body + Gallery arrange selects + Gallery search use primitives / `Input type="search"`
- [x] 2.3 Naira prefix + `inputMode` on fee/paid fields; ImageSheet Saved uses a Studio token
- [x] 2.4 Settings notice checkboxes: 44px hits; leftover boxed `bg-studio-bg` fields → panel or Textarea

## 3. Check

- [x] 3.1 Verify light + night: Bookings create, Clients notes, Gallery search + photo sheet, Settings notices — focus ring and contrast
